# Skills extraction prompts

Versioned templates for Pass 2 (LLM-based) skills extraction. `SKILLS_PROMPT_VERSION` in each module is used for Exercise 4.5 prompt iteration.

When you change a prompt, document the change and before/after metrics in `agents/eval/prompt_iteration_log.md`.
