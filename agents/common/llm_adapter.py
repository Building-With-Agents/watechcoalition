"""LLM adapter — provider-agnostic; get_adapter(provider) returns adapter for complete()."""


def get_adapter(provider=None):
    """Return the configured LLM adapter. Phase 1: stub."""
    return None
